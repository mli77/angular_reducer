# NgrxMultipleReducers

Demonstrating multiple reducers.

export NODE_OPTIONS=--openssl-legacy-provider


`ng new ngrx-multiple-reducers --routing=false --style=css`

`cd ngrx-multiple-reducers/`

`ng add @ngrx/store@latest`

`ng serve`